

<div class = "container">
  <h2> Filter by      <a href="#" onclick="return false;" data-toggle="popover" 
          data-trigger="focus" data-placement="right"  title="How it works?" 
          data-content="1) Select from the dropdown menu the filters you want to use for your search. You can filter for 1 category, for all 3 or combine them as you like.
                        2) Click on search button. The results appear above the filter. 3) Click on the Echogram image to see the details.">
          <span class="glyphicon glyphicon-info-sign" style="font-size:20px;"></span></a> </h2> 

    <div class = "row" >
      <div class = "col-sm-6">
        <%= form_tag( {}, {method: "get", id: "filter"}) do %>
            <%= label_tag "Species:" %>
            <%= select_tag "species", options_for_select(@species_list) %>
            <%= label_tag "Mean length:" %>
            <%= select_tag "avglength", options_for_select(@avglength_list) %>
            <%= label_tag "Percentage:" %>
            <%= select_tag "percent", options_for_select(@percent_list) %>
            <%= submit_tag("Search", class: "btn btn-default btn-lg") %>
        <% end %>
      </div> 
    </div>
</div>



<div class="container">
    <h5> Results found <br><br> <%= @text_spec %> </h5>
   

    <%if @count != 0 %>
        <%= render "all_records" %>
    <% end %>

</div>


<script>
$(function () {
  $('[data-toggle="popover"]').popover()
})
</script>
