<div class="table-responsive">
  <table class="table table-hover">
    <thead>
      <tr>
        <th>Echogram Image</th>
        <th>Date</th>       
        <th>Species</th>
        <th>Percentage</th>
        <th>MeanLength</th>
        <th>Longitude</th>
        <th>Latitude</th>
        <th>More Info</th>
        <th colspan="10"></th>
      </tr>
    </thead>

    <tbody>
      <% @display.each do |item| %>
        <tr>
          <td>
              <% name = "/images/" + item.gramname.to_s %>
              <%= image_tag(name, options = {size: "200x150"})%>           
          </td>
          
          <td> 
            <% temp = @h["#{item.gramname.to_s }"].to_s %> 
            <% arr = [] %> 
            <% arr = temp.split(",").map { |s| s.to_s } %> 
            <% arr.each do |r| %> 
              <%= r %> <br><br>
            <% end %>      
          </td>

          <td> <%= item.long %> </td>
          <td> <%= item.lat %> </td>
          <td> <%= "Details" %> </td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
<%= will_paginate @display%>